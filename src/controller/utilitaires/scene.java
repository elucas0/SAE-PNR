package controller.utilitaires;

/**
 * Class that creates a scene
 */
public class scene {

    /**
     * Main method that creates a scene
     * @param args the arguments
     */
    public static void main(String[] args){

        CreerCompte c = new CreerCompte();
        c.faireComptes();
    }
    
}
